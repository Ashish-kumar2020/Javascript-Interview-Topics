# 4 - How Functions Works in Javascript

Below is the Execution For the Above Code Snippet

Initial Phase : where js defines variable and functions in the memory

After this variable are assigned with the value.

After this Javascript starts executing the code.

when the call reaches to a() function a whole new execution context is created inside the Global Exection Context.

Then the Code componet phase start the function a()

Once the execution of a() gets completed it gets poped out of the call stack.

Now the Execution start for the b()

A new execution context is created inside the global execution context.

In the memory creation phase all the variables and functions are assigned with some placeholder(varibale) and functions get copy pasted.

In the next the phase the variable gets the value.

After this Code creation phase started and the final value gets logged in the console.

once the execution of b() gets completed it gets poped out of the call stack.

Once both the function call get executed now the console.log(x) gets printed to the console.

Once all the data gets executed the GEC(Global Execution Context) gets popped out from the call stack.

Link for Future Refernce

