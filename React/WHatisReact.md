# What is React?
-> React is javascript library used to build user interfaces,expecially singe page application, it follows a component based architecture it can be class or function, where the Ui is divided into re-usable components. react is decalrative in nature means we developer have to only describe how the UI will look in current state , react will handle the rendering part


Imprtant Feature of React
 1 - Virtual DOM
 2 - JSX Syntx
 3 - One Way data binding
 4 - COmponent Based architecture
 5 - Decalrative UI

# 1. Explaining React’s role in a web application during a technical round.
# -> React primary role is build user interfaces, it follows component based architecture, we can create re-usalbe componets such as header, navbar,dashboard etc which helps in making the application sclable

# 2. Why is React preferred over other libraries/frameworks for Single Page Applications (SPAs)?
# -> React is preferred for SPA because of its component based architecture which makes code modular and reusable. it uses the reconciliation process which efficiently update the UI.

# 3. How do reusable components help scale applications?
# -> Reusbale components helps in reducing the duplicate code and makes code more modular, instead of writing the same UI in multiple place it makes lots of duplicate code so what we can do is create component with a common logic and we can use that component whenver we need it avoids code duplicacy.also it is easier while fixing a bug or updating the UI.

# 4. Talking about performance optimizations using the Virtual DOM in a system design interview.
# -> 