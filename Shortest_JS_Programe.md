# 5 - Shortest JavaScript Program

The Shortest JavaScript program is an Empty JavaScript File.

When the file runs javascript runs all the process, it creates Global Execution Context, window object and this keyword.

At global level window object === GEC

Both window object and this keyword are created at the time of creation of Global Execution Context.

NOTE : Anything which is not inside the function scope it is in Global Space

